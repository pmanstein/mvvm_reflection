name: resolve_di
description: "Dependency Injection with Reflecatble"
resolution: workspace
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  reflectable: ^5.0.1
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.5.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  reflectable_builder: ^1.0.1
  command_it: ^9.5.1

flutter:
