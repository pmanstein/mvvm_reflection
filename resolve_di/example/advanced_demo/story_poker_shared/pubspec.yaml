name: story_poker_shared
description: "Shared Result type for wrapping success/error values."
resolution: workspace
publish_to: "none"

version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
