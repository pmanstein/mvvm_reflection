name: story_poker_features
description: "Feature modules (MVVM: data/domain/presentation) for the advanced_demo app."
resolution: workspace
publish_to: "none"

version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  command_it: ^9.5.1
  logging: ^1.3.0
  resolve_di: ^0.0.1
  story_poker_shared: ^0.1.0

dev_dependencies:
  build_runner: ^2.5.4
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.3.0

flutter:
  uses-material-design: true
