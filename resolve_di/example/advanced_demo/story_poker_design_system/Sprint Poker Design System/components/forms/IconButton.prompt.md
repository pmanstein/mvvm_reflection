Square icon-only button for toolbars (copy link, settings, mute, kebab menu).

```jsx
<IconButton icon="⚙" label="Settings" onClick={openSettings} />
```

`active` highlights the button (e.g. toggled state). `variant="outline"` for isolated icon actions on a plain surface.
