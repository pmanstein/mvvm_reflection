The core planning-poker card. Used in the voting deck (idle, clickable), as the user's own pick (selected — accent glow), as a placeholder for other voters before reveal (facedown), and in the results grid after reveal (revealed).

```jsx
<VoteCard value="8" state="selected" onClick={()=>vote('8')} />
```
